* RPCs (remote procedure calls)

-  suppose user have to login then and authenticate
- when user fill the form then this will get notification for otp and their authentication service is come
- so in notification service have one http call function like notification_otp which is talk notification service and sent to otp and end point is exposed by that service
	
* core problems
- suppose our service is interact with 10 or above service then we have to call that much api calls?
and also we have to handle cased when got any type of errors and retries
- suppose 10 other services is interacting with notification services and all written in different languages than api call structure should be different for that all services?
 and RPC comes in picture
 
* what if we abstract out the mundane task and repetitive tasks like communication protocol, object creation, retries etc. and just developer need to focus on business logics 
this is how RPC was worked

* RPC is all about communicate other services no matter what language is used and which library used 
which is looks like a regular function call is injact a remote precedure call that happens over the netwrok throgh stubs.
core idea is hide the complexity of a remote call.  

* befor we jump what rpc is lets look on stubs what is stubs 
login is on the auth service
notfication otp is on the nofigication srvc 
so here someone needs to convert (serialize) the information from auth service in to some format that can be sent oer the wire and convert it back to native objects on notification service
and this work is done by  stub

* stub interface description language
we are define .proto file for services and for same we are defining types in both service in login and notification services
and we run generater and create code for as for any language like java, node, rust, go 
generater take .proto files and generate .go and .java and .rs files

* now communication in rpc
rpc can use any layer 3 communication protocol tcp or udp or http

* why rpc 
- easy to use and strong api contract 
- invoke remote call just like a local call boosts dev productivity 
- most rpc frameworks like grpc. supports most modern languages 
- most mundane tasks are abstracked 
	- 
- performancce out of the box
	compression, multiplexing, efficient payload transfer, connection pool, streaming

* concerns 
- stubs need to be regerated when signature changes
- testing rpc is not-trival
- getting started is a little challenging
- browser support is limited

slack as api is grpc based

